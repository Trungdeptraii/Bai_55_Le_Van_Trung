<div class="row justify-content-center">
    <div class="col-3">
        <form action="/users/delete/<%= user.id %>" method="post" class="alert alert-danger ">
            <p>Bạn có muốn xóa tài khoản <%- `<b>${user.name}</b>` %> không ?</p>
            <button type="button" class="btn btn-primary">
                <a href="/users/" style="color: white; text-decoration: none;">Quay lại</a>
            </button>
            <button type="submit" class="btn btn-danger">Xóa</button>
        </form>
    </div>
</div>